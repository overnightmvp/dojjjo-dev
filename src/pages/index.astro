---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Decrease the time between idea and execution" description="DOJJJO is the Fractional Product Studio for multi-hyphenate builders. We build the infrastructure that makes you inevitable.">
	<main>
		<!-- HERO SECTION: THE STUDIO ENGINE -->
		<section class="hero-section">
			<div class="container hero-grid">
				<div class="hero-text animate-fade-in">
					<div class="protocol-tag tech-line line-v">FRACTIONAL_PRODUCT_STUDIO_V1</div>
					<h1 class="text-gradient">
						Decrease the time<br />
						between idea and<br />
						<span class="text-accent">Execution.</span>
					</h1>
					<p class="hero-desc">
						Stop hiring generic agencies that don't scale. You need a <strong>Fractional Product Specialist</strong> to handle the code, the authority, and the shipping. We build the infrastructure that makes your vision inevitable.
					</p>
					<div class="hero-cta-group">
						<a href="/audit" class="btn btn-primary" data-astro-prefetch>Run Product Audit</a>
						<a href="/work" class="btn btn-outline" data-astro-prefetch>Browse Selected Work</a>
					</div>
				</div>

				<div class="hero-visual animate-fade-in" style="animation-delay: 0.2s;">
					<div class="terminal-mockup">
						<div class="terminal-header">
							<span class="dot"></span><span class="dot"></span><span class="dot"></span>
							<span class="terminal-title">DOJJJO_EXECUTION_ENGINE.EXE</span>
						</div>
						<div class="terminal-body">
							<p class="query">> initialize_studio_stack --client="[Your_Brand]"</p>
							<p class="thinking">_Syncing Budo Principles...</p>
							<div class="answer-box">
								<p>✔ Design System: SYST_01 [READY]</p>
								<p>✔ Authority Matrix: ACTIVE [INJECTED]</p>
								<p>✔ Shipping Velocity: 10x [OPTIMIZED]</p>
							</div>
							<p class="source-list">Result: INFRASTRUCTURE_STABLE. READY_TO_SHIP.</p>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- TRUST BAR: PRODUCTION LINES -->
		<section class="trust-bar surface">
			<div class="container flex-center gap-xl">
				<span class="trust-label">Infrastructure Deployed For:</span>
				<div class="logo-strip">
					<span class="logo-placeholder">Twine</span>
					<span class="logo-placeholder">Caption</span>
					<span class="logo-placeholder">Stealth_SaaS</span>
					<span class="logo-placeholder">Builder_Network</span>
				</div>
			</div>
		</section>

		<!-- THE DILEMMA: WHO WE SERVE -->
		<section class="section container line-h">
			<div class="section-meta text-error">EXECUTION_GAP_ANALYSIS</div>
			<h2 class="text-gradient">The Builder's Bottleneck</h2>
			<div class="icp-grid">
				<div class="icp-card surface">
					<div class="icp-icon">⬢</div>
					<h3>The Visionary</h3>
					<p>You have the audience, but you're trapped in technical debt or slow agency cycles. You need a high-velocity partner.</p>
					<a href="/services/fractional-product-specialist" class="link-arrow" data-astro-prefetch>Retain Specialist</a>
				</div>
				<div class="icp-card surface">
					<div class="icp-icon">⬡</div>
					<h3>The Invisible Expert</h3>
					<p>You're the authority, but search engines and AI agents don't see you. We build the visibility infrastructure you're missing.</p>
					<a href="/services/authority-loop" class="link-arrow" data-astro-prefetch>Inject Authority</a>
				</div>
				<div class="icp-card surface">
					<div class="icp-icon">⬣</div>
					<h3>The Scale Founder</h3>
					<p>Your product works, but your outbound is manual and messy. We automate the distribution and SDR process.</p>
					<a href="/services/outbound" class="link-arrow" data-astro-prefetch>Automate Outbound</a>
				</div>
			</div>
		</section>

		<!-- AUTHORITY TICKER: DOCTRINE -->
		<section class="ticker-section">
			<div class="ticker-wrapper">
				<div class="ticker-content">
					<span class="ticker-item text-error">⚠ DOCTRINE: SEIRYOKU ZENYO (MAXIMUM EFFICIENCY)</span>
					<span class="ticker-item">● JITA KYOEI (MUTUAL PROSPERITY)</span>
					<span class="ticker-item">● FUDOSHIN (UNSHAKABLE MIND)</span>
					<span class="ticker-item text-accent">● SYSTEM OVER CHAOS</span>
				</div>
			</div>
		</section>

		<!-- THE SERVICE HIERARCHY -->
		<section class="section" style="background: var(--color-surface-soft);">
			<div class="container">
				<div class="split-header">
					<div>
						<div class="section-meta">STUDIO_HIERARCHY</div>
						<h2 class="text-gradient">The Execution Stack</h2>
					</div>
					<p class="header-desc">From authority to product. Choose your injection point.</p>
				</div>

				<div class="value-grid">
					<!-- TIER 1: AUTHORITY LOOP -->
					<div class="value-column">
						<div class="tier-label">THE_AUTHORITY_LOOP // FLAGSHIP</div>
						<h3>Be Seen.</h3>
						<p>Force AI agents and search engines to cite you as the default choice in your niche.</p>
						<ul class="feature-list">
							<li>30-Day Entity Injection</li>
							<li>Agent-Native <code>llms.txt</code></li>
							<li>Signal Integrity Audit</li>
						</ul>
						<a href="/services/authority-loop" class="btn btn-outline w-full" data-astro-prefetch>Initialize Authority</a>
					</div>

					<!-- TIER 2: PRODUCT SPECIALIST (Core) -->
					<div class="value-column featured">
						<div class="tier-label accent">PRODUCT_SPECIALIST // CORE_OFFER</div>
						<h3>Be Built.</h3>
						<p>Design, Code, Strategy, and Ship. Your Fractional CPO for high-velocity builders.</p>
						<ul class="feature-list">
							<li>Full-Stack MVP Development</li>
							<li>Proprietary Design Systems</li>
							<li>Systematic Product Roadmap</li>
						</ul>
						<a href="/services/fractional-product-specialist" class="btn btn-primary w-full" data-astro-prefetch>Retain Specialist</a>
					</div>

					<!-- TIER 3: CAPTION OUTBOUND -->
					<div class="value-column">
						<div class="tier-label">CAPTION_OUTBOUND // DISTRIBUTION</div>
						<h3>Be Sold.</h3>
						<p>Automated hyper-outbound and SDR frameworks to convert your new authority into contracts.</p>
						<ul class="feature-list">
							<li>AI-Driven Lead Scraping</li>
							<li>Authority-First Scripts</li>
							<li>Automated Follow-up Loops</li>
						</ul>
						<a href="/services/outbound" class="btn btn-outline w-full" data-astro-prefetch>Deploy Outbound</a>
					</div>
				</div>
			</div>
		</section>

		<!-- THE PHILOSOPHY: BUDO-INSPIRED EXECUTION -->
		<section class="protocols-showcase section container">
			<div class="section-meta">STUDIO_PHILOSOPHY</div>
			<h2 class="text-gradient">The DOJJJO Way</h2>
			<p class="section-desc">We operate on Budo principles. Not for the aesthetic, but for the efficiency. Minimal waste, maximal impact.</p>

			<div class="philosophy-grid">
				<div class="philosophy-card surface reveal-on-scroll">
					<span class="protocol-num">01 // SEIRYOKU ZENYO</span>
					<h3>Maximum Efficiency</h3>
					<p>We don't sell 'hours'. We sell 'velocity'. If it doesn't move the needle, we don't build it.</p>
				</div>
				<div class="philosophy-card surface reveal-on-scroll" style="animation-delay: 0.1s;">
					<span class="protocol-num">02 // JITA KYOEI</span>
					<h3>Mutual Prosperity</h3>
					<p>We are not a vendor. We are a part of your team. Our success is measured by your shipping speed.</p>
				</div>
				<div class="philosophy-card surface reveal-on-scroll" style="animation-delay: 0.2s;">
					<span class="protocol-num">03 // FUDOSHIN</span>
					<h3>Unshakable Mind</h3>
					<p>Technical chaos is expensive. We build rigid, clean infrastructures that allow for fluid creativity.</p>
				</div>
			</div>
		</section>

		<!-- FINAL CALL -->
		<section class="section container text-center">
			<div class="protocol-tag">FINAL_CALL</div>
			<h2 class="text-gradient" style="font-size: 3.5rem;">Ready to Ship?</h2>
			<p class="hero-desc" style="margin-left: auto; margin-right: auto;">
				The gap between your idea and its execution is where dreams die. Let's close it today.
			</p>
			<a href="/audit" class="btn btn-primary btn-lg">Run Product Readiness Audit</a>
		</section>
	</main>
</Layout>

<style>
	/* Optimized Layout & Sections */
	.hero-section {
		padding: var(--space-xl) 0 var(--space-lg);
		background: radial-gradient(circle at top right, var(--color-accent-glow) 0%, transparent 40%);
		min-height: 80vh;
		display: flex;
		align-items: center;
	}

	.hero-grid {
		display: grid;
		grid-template-columns: 1.1fr 0.9fr;
		gap: var(--space-xl);
		align-items: center;
	}

	.protocol-tag {
		font-family: monospace;
		color: var(--color-accent);
		font-size: 0.8125rem;
		letter-spacing: 0.2em;
		margin-bottom: 1.5rem;
	}

	.hero-desc {
		font-size: 1.25rem;
		color: var(--color-text-secondary);
		margin: var(--space-md) 0;
		line-height: 1.6;
		max-width: 600px;
	}

	.hero-cta-group {
		display: flex;
		gap: 1rem;
		margin-top: 2rem;
	}

	/* Terminal Mockup */
	.terminal-mockup {
		background: #000;
		border: 1px solid var(--color-border);
		border-radius: 8px;
		overflow: hidden;
		box-shadow: 0 30px 60px rgba(0,0,0,0.5), 0 0 30px var(--color-accent-glow);
	}

	.terminal-header {
		background: var(--color-surface);
		padding: 0.75rem 1rem;
		border-bottom: 1px solid var(--color-border);
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	.terminal-header .dot {
		width: 8px;
		height: 8px;
		border-radius: 50%;
		background: var(--color-border);
	}

	.terminal-title {
		font-size: 0.7rem;
		font-family: monospace;
		color: var(--color-text-secondary);
		margin-left: 1rem;
		letter-spacing: 0.1em;
	}

	.terminal-body {
		padding: 1.5rem;
		font-family: monospace;
		font-size: 0.9rem;
	}

	.query { color: var(--color-text-secondary); }
	.thinking { color: var(--color-accent); margin: 1rem 0; opacity: 0.7; }
	.answer-box {
		border-left: 2px solid var(--color-accent);
		padding-left: 1rem;
		margin: 1.5rem 0;
		color: #fff;
	}

	.source-list { font-size: 0.75rem; color: var(--color-text-secondary); margin-top: 2rem; }

	/* Trust Bar */
	.trust-bar { padding: var(--space-md) 0; border-top: 1px solid var(--color-border); border-bottom: 1px solid var(--color-border); }
	.trust-label { font-size: 0.75rem; color: var(--color-text-secondary); opacity: 0.5; font-family: monospace; letter-spacing: 0.1em; }
	.logo-strip { display: flex; align-items: center; gap: var(--space-xl); opacity: 0.6; filter: grayscale(1); }
	.logo-placeholder { font-family: var(--font-heading); font-weight: 800; font-size: 1.2rem; color: #fff; letter-spacing: -0.05em; }

	/* ICP Grid */
	.icp-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--space-md);
		margin-top: var(--space-lg);
	}

	.icp-card {
		padding: var(--space-lg) var(--space-gutter);
		border: 1px solid var(--color-border);
		border-radius: 12px;
		transition: var(--transition-smooth);
	}

	.icp-card:hover { border-color: var(--color-accent); transform: translateY(-5px); }
	.icp-icon { font-size: 2rem; color: var(--color-accent); margin-bottom: 1rem; }
	.icp-card h3 { margin-bottom: 1rem; }
	.icp-card p { color: var(--color-text-secondary); margin-bottom: 2rem; font-size: 0.9rem; }

	.link-arrow { color: var(--color-accent); font-weight: 700; font-size: 0.8rem; display: flex; align-items: center; }
	.link-arrow::after { content: ' →'; }

	/* Value Ladder */
	.split-header {
		display: flex;
		justify-content: space-between;
		align-items: flex-end;
		margin-bottom: var(--space-xl);
	}

	.header-desc { color: var(--color-text-secondary); font-size: 1.1rem; }

	.value-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--space-lg);
	}

	.value-column {
		padding: var(--space-lg) var(--space-gutter);
		background: var(--color-bg);
		border: 1px solid var(--color-border);
		border-radius: 16px;
		display: flex;
		flex-direction: column;
	}

	.value-column.featured {
		border-color: var(--color-accent);
		background: var(--color-surface);
		transform: scale(1.05);
		box-shadow: 0 0 40px var(--color-accent-glow);
		z-index: 10;
	}

	.tier-label { font-family: monospace; font-size: 0.75rem; font-weight: 800; margin-bottom: 1.5rem; color: var(--color-text-secondary); opacity: 0.6; }
	.tier-label.accent { color: var(--color-accent); opacity: 1; }

	.value-column h3 { font-size: 2rem; margin-bottom: 1rem; }
	.value-column p { color: var(--color-text-secondary); margin-bottom: 2rem; }

	.feature-list { list-style: none; padding: 0; margin: 0 0 3rem 0; flex-grow: 1; }
	.feature-list li { margin-bottom: 1rem; padding-left: 1.5rem; position: relative; color: var(--color-text-secondary); font-size: 0.95rem; }
	.feature-list li::before { content: '▨'; position: absolute; left: 0; color: var(--color-accent); }
	
	.w-full { width: 100%; }

	/* Philosophy Section */
	.philosophy-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--space-md);
		margin-top: var(--space-lg);
	}
	.philosophy-card { padding: var(--space-lg) var(--space-gutter); border-radius: 12px; }
	.philosophy-card .protocol-num { font-family: monospace; font-size: 0.7rem; color: var(--color-accent); display: block; margin-bottom: 0.5rem; opacity: 0.7; }
	.philosophy-card h3 { font-size: 1.5rem; margin-bottom: 1rem; }
	.philosophy-card p { color: var(--color-text-secondary); font-size: 0.95rem; line-height: 1.6; }

	/* Ticker */
	.ticker-section { padding: var(--space-md) 0; background: #000; overflow: hidden; border-top: 1px solid var(--color-border); border-bottom: 1px solid var(--color-border); }
	.ticker-wrapper { display: flex; white-space: nowrap; animation: ticker 40s linear infinite; }
	.ticker-content { display: flex; gap: var(--space-xl); }
	.ticker-item { font-family: var(--font-heading); font-weight: 900; font-size: 1rem; letter-spacing: 0.1em; color: var(--color-text-secondary); }

	@keyframes ticker {
		0% { transform: translateX(0); }
		100% { transform: translateX(-50%); }
	}

	/* Responsive Refinements */
	@media (max-width: 992px) {
		.hero-grid { grid-template-columns: 1fr; text-align: center; gap: var(--space-lg); }
		.hero-desc { margin: var(--space-md) auto; }
		.hero-cta-group { justify-content: center; }
		.hero-visual { max-width: 500px; margin: 0 auto; }
		.value-grid { grid-template-columns: 1fr; gap: var(--space-md); }
		.value-column.featured { transform: none; }
		.philosophy-grid { grid-template-columns: 1fr; }
	}
	
	@media (max-width: 640px) {
		.hero-section { padding: var(--space-lg) 0; }
		.hero-cta-group { flex-direction: column; }
		.icp-grid { grid-template-columns: 1fr; }
		.logo-strip { gap: var(--space-md); justify-content: center; flex-wrap: wrap; }
		.section { padding: var(--space-lg) 0; }
		.split-header { flex-direction: column; align-items: flex-start; gap: 0.5rem; margin-bottom: var(--space-lg); }
		.value-column { padding: var(--space-lg); }
	}
</style>
