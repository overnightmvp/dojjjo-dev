---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Decrease the time between idea and execution" description="DOJJJO is the Fractional Product Studio for multi-hyphenate builders. We build the infrastructure that makes you inevitable.">
	<main>
		<!-- HERO SECTION: THE BUILDER'S OS -->
		<section class="hero-section">
			<div class="container hero-grid">
				<div class="hero-text animate-fade-in">
					<div class="protocol-tag tech-line line-v">SYSTEMATIC_EXECUTION_PROTOCOL</div>
					<h1 class="text-gradient">
						Decrease the time<br />
						between idea and<br />
						<span class="text-accent">Execution.</span>
					</h1>
					<p class="hero-desc">
						You don't need another generic agency. You need a <strong>Fractional Product Specialist</strong> who understands the code, the authority, and the market. We build the infrastructure that makes you inevitable.
					</p>
					<div class="hero-cta-group">
						<a href="/audit" class="btn btn-primary" data-astro-prefetch>Run Product Audit</a>
						<a href="/work" class="btn btn-outline" data-astro-prefetch>View Production Lines</a>
					</div>
				</div>

				<div class="hero-visual animate-fade-in" style="animation-delay: 0.2s;">
					<div class="terminal-mockup">
						<div class="terminal-header">
							<span class="dot"></span><span class="dot"></span><span class="dot"></span>
							<span class="terminal-title">DOJJJO_EXECUTION_ENGINE.EXE</span>
						</div>
						<div class="terminal-body">
							<p class="query">> deploy_authority_matrix --target="[Your_Brand]"</p>
							<p class="thinking">_Initializing build sequence...</p>
							<div class="answer-box">
								<p>✔ Phase 1: Product Infrastructure [DEPLOYED]</p>
								<p>✔ Phase 2: Authority Signals [INJECTED]</p>
								<p>✔ Phase 3: Market Dominance [ACTIVE]</p>
							</div>
							<p class="source-list">Status: 100% EXECUTED. GAP ALIGNED.</p>
						</div>
					</div>
				</div>
			</div>
		</section>

		<!-- TRUST BAR: BUILDERS & BRANDS -->
		<section class="trust-bar surface">
			<div class="container flex-center gap-xl">
				<span class="trust-label">Infrastructure Deployed For:</span>
				<div class="logo-strip">
					<span class="logo-placeholder">Twine</span>
					<span class="logo-placeholder">Caption</span>
					<span class="logo-placeholder">Stealth_Startup_01</span>
					<span class="logo-placeholder">Confidential_Series_B</span>
				</div>
			</div>
		</section>

		<section class="section container line-h">
			<div class="section-meta text-error">EXECUTION_GAP_DETECTED</div>
			<h2 class="text-gradient">The Builder's Dilemma</h2>
			<div class="icp-grid">
				<div class="icp-card surface">
					<div class="icp-icon">⬢</div>
					<h3>The Visionary</h3>
					<p>You have the idea and the audience, but you're trapped in "No-Code Hell" or filtered by expensive devs who don't 'get' marketing.</p>
					<a href="/services/fractional-product-specialist" class="link-arrow" data-astro-prefetch>Deploy Specialist</a>
				</div>
				<div class="icp-card surface">
					<div class="icp-icon">⬡</div>
					<h3>The Invisible Expert</h3>
					<p>You're the smartest person in the room, but the AI agents cite your VC-backed competitor because they have better schema.</p>
					<a href="/services/trust-authority-loop" class="link-arrow" data-astro-prefetch>Claim Authority</a>
				</div>
				<div class="icp-card surface">
					<div class="icp-icon">⬣</div>
					<h3>The Sales-Led Founder</h3>
					<p>Your team is grinding on phones, but your total addressable market is invisible to their procurement bots.</p>
					<a href="/services/outbound" class="link-arrow" data-astro-prefetch>Fix Distribution</a>
				</div>
			</div>
		</section>

		<!-- AUTHORITY TICKER: DOCTRINE -->
		<section class="ticker-section">
			<div class="ticker-wrapper">
				<div class="ticker-content">
					<span class="ticker-item text-error">⚠ DOCTRINE: SEIRYOKU ZENYO (MAXIMUM EFFICIENCY)</span>
					<span class="ticker-item">● JITA KYOEI (MUTUAL PROSPERITY)</span>
					<span class="ticker-item">● FUDOSHIN (UNSHAKABLE MIND)</span>
					<span class="ticker-item text-accent">● SYSTEM OVER CHAOS</span>
				</div>
			</div>
		</section>

		<!-- THE SERVICE HIERARCHY -->
		<section class="section" style="background: var(--color-surface-soft);">
			<div class="container">
				<div class="split-header">
					<div>
						<div class="section-meta">SERVICE_ARCHITECTURE</div>
						<h2 class="text-gradient">The Execution Stack</h2>
					</div>
					<p class="header-desc">From authority to product. Choose your injection point.</p>
				</div>

				<div class="value-grid">
					<!-- TIER 3: OUTBOUND -->
					<div class="value-column">
						<div class="tier-label">CAPTION_OUTBOUND // $2K-$5K</div>
						<h3>Distribution</h3>
						<p>Authority without distribution is just noise. We convert your citations into contracts via hyper-outbound.</p>
						<ul class="feature-list">
							<li>Technical Audit Hooks</li>
							<li>"Gap Report" Campaigns</li>
							<li>SDR Process Automation</li>
						</ul>
						<a href="/services/outbound" class="btn btn-outline w-full" data-astro-prefetch>Initialize Outbound</a>
					</div>

					<!-- TIER 1: AUTHORITY LOOP -->
					<div class="value-column">
						<div class="tier-label">AUTHORITY_LOOP // $5.5K+</div>
						<h3>Authority</h3>
						<p>The flagship protocol. We verify your entity and force AI agents to recommend you as the default choice.</p>
						<ul class="feature-list">
							<li>30-Day Knowledge Graph Injection</li>
							<li>Agent-Native <code>llms.txt</code></li>
							<li>High-DA Trust Anchors</li>
						</ul>
						<a href="/services/trust-authority-loop" class="btn btn-outline w-full" data-astro-prefetch>Loop Access</a>
					</div>

					<!-- TIER 2: PRODUCT SPECIALIST (Main) -->
					<div class="value-column featured">
						<div class="tier-label accent">PRODUCT_SPECIALIST // $8K-$15K</div>
						<h3>Production</h3>
						<p>Your Fractional CPO. Design, Code, Strategy, and Ship. We build the actual product infrastructure you need to scale.</p>
						<ul class="feature-list">
							<li>Full-Stack Development (Astro/React)</li>
							<li>Systematic Design Systems</li>
							<li>Product Roadmap Execution</li>
						</ul>
						<a href="/services/fractional-product-specialist" class="btn btn-primary w-full" data-astro-prefetch>Retain Specialist</a>
					</div>
				</div>
			</div>
		</section>

		<!-- THE PROTOCOLS: PHILOSOPHY -->
		<section class="protocols-showcase">
			<div class="container">
				<div class="section-meta">OPERATING_SYSTEM</div>
				<h2 class="text-gradient">The DOJJJO Protocol</h2>
				<p class="section-desc">We operate on a strict set of Budo-inspired principles. No fluff. No jargon. just systematic execution.</p>

				<!-- 01: SEIRYOKU ZENYO -->
				<div class="protocol-story line-h reveal-on-scroll">
					<div class="story-content">
						<span class="protocol-num">PRINCIPLE_01</span>
						<h3>Seiryoku Zenyo</h3>
						<p class="story-pain"><strong>Translation:</strong> Maximum Efficiency.</p>
						<p class="story-heaven">We do not believe in "hours worked." We believe in "velocity of shipping." Every line of code and every piece of content is engineered for maximum leverage.</p>
					</div>
					<div class="story-stat surface">
						<div class="stat-value">100%</div>
						<div class="stat-label">Leverage on every asset deployed.</div>
					</div>
				</div>

				<!-- 02: JITA KYOEI -->
				<div class="protocol-story line-h reveal-on-scroll">
					<div class="story-content">
						<span class="protocol-num">PRINCIPLE_02</span>
						<h3>Jita Kyoei</h3>
						<p class="story-pain"><strong>Translation:</strong> Mutual Prosperity.</p>
						<p class="story-heaven">The agency model is broken because incentives are misaligned. We win when you ship. We scale when you sell. We are your partner, not your vendor.</p>
					</div>
					<div class="story-stat surface">
						<div class="stat-value">PARTNER</div>
						<div class="stat-label">Fractional Ownership Mindset.</div>
					</div>
				</div>

				<!-- 03: FUDOSHIN -->
				<div class="protocol-story last line-h reveal-on-scroll">
					<div class="story-content">
						<span class="protocol-num">PRINCIPLE_03</span>
						<h3>Fudoshin</h3>
						<p class="story-pain"><strong>Translation:</strong> The Unshakable Mind.</p>
						<p class="story-heaven">Markets change. Algorithms shift. AI evolves. We build rigid frameworks that allow for fluid creativity, ensuring your foundation never cracks under pressure.</p>
					</div>
					<div class="story-stat surface">
						<div class="stat-value">STABLE</div>
						<div class="stat-label">Infrastructure built to last.</div>
					</div>
				</div>

			</div>
		</section>

		<!-- FINAL CONVICTION -->
		<section class="section container text-center">
			<div class="protocol-tag">FINAL_CALL</div>
			<h2 class="text-gradient" style="font-size: 3.5rem;">Stop Thinking. Start Shipping.</h2>
			<p class="hero-desc" style="margin-left: auto; margin-right: auto;">
				The gap between your idea and its execution is where dreams die. Close the gap.
			</p>
			<a href="/audit" class="btn btn-primary btn-lg">Run Product Readiness Audit</a>
		</section>
	</main>
</Layout>

<style>
	/* Optimized Layout & Sections */
	.hero-section {
		padding: var(--space-xl) 0;
		background: radial-gradient(circle at top right, var(--color-accent-glow) 0%, transparent 40%);
		min-height: 85vh;
		display: flex;
		align-items: center;
	}

	.hero-grid {
		display: grid;
		grid-template-columns: 1.1fr 0.9fr;
		gap: var(--space-xl);
		align-items: center;
	}

	.protocol-tag {
		font-family: monospace;
		color: var(--color-accent);
		font-size: 0.8125rem;
		letter-spacing: 0.2em;
		margin-bottom: 1.5rem;
	}

	.hero-desc {
		font-size: 1.25rem;
		color: var(--color-text-secondary);
		margin: var(--space-md) 0;
		line-height: 1.6;
		max-width: 600px;
	}

	.hero-cta-group {
		display: flex;
		gap: 1rem;
		margin-top: 2rem;
	}

	/* Terminal Mockup */
	.terminal-mockup {
		background: #000;
		border: 1px solid var(--color-border);
		border-radius: 8px;
		overflow: hidden;
		box-shadow: 0 30px 60px rgba(0,0,0,0.5), 0 0 30px var(--color-accent-glow);
	}

	.terminal-header {
		background: var(--color-surface);
		padding: 0.75rem 1rem;
		border-bottom: 1px solid var(--color-border);
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	.terminal-header .dot {
		width: 8px;
		height: 8px;
		border-radius: 50%;
		background: var(--color-border);
	}

	.terminal-title {
		font-size: 0.7rem;
		font-family: monospace;
		color: var(--color-text-secondary);
		margin-left: 1rem;
		letter-spacing: 0.1em;
	}

	.terminal-body {
		padding: 1.5rem;
		font-family: monospace;
		font-size: 0.9rem;
	}

	.query { color: var(--color-text-secondary); }
	.thinking { color: var(--color-accent); margin: 1rem 0; opacity: 0.7; }
	.answer-box {
		border-left: 2px solid var(--color-accent);
		padding-left: 1rem;
		margin: 1.5rem 0;
		color: #fff;
	}

	.source-list { font-size: 0.75rem; color: var(--color-text-secondary); margin-top: 2rem; }

	/* ICP Grid */
	.icp-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--space-md);
		margin-top: var(--space-lg);
	}

	.icp-card {
		padding: var(--space-lg);
		border: 1px solid var(--color-border);
		border-radius: 12px;
		transition: var(--transition-smooth);
	}

	.icp-card:hover { border-color: var(--color-accent); transform: translateY(-5px); }
	.icp-icon { font-size: 2rem; color: var(--color-accent); margin-bottom: 1rem; }
	.icp-card h3 { margin-bottom: 1rem; }
	.icp-card p { color: var(--color-text-secondary); margin-bottom: 2rem; font-size: 0.9rem; }

	.link-arrow { color: var(--color-accent); font-weight: 700; font-size: 0.8rem; display: flex; align-items: center; }
	.link-arrow::after { content: ' →'; }

	/* Value Ladder */
	.split-header {
		display: flex;
		justify-content: space-between;
		align-items: flex-end;
		margin-bottom: var(--space-xl);
	}

	.header-desc { color: var(--color-text-secondary); font-size: 1.1rem; }

	.value-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--space-lg);
	}

	.value-column {
		padding: var(--space-xl);
		background: var(--color-bg);
		border: 1px solid var(--color-border);
		border-radius: 16px;
		display: flex;
		flex-direction: column;
	}

	.value-column.featured {
		border-color: var(--color-accent);
		background: var(--color-surface);
		transform: scale(1.05);
		box-shadow: 0 0 40px var(--color-accent-glow);
		z-index: 10;
	}

	.tier-label { font-family: monospace; font-size: 0.75rem; font-weight: 800; margin-bottom: 1.5rem; color: var(--color-text-secondary); opacity: 0.6; }
	.tier-label.accent { color: var(--color-accent); opacity: 1; }

	.value-column h3 { font-size: 2rem; margin-bottom: 1rem; }
	.value-column p { color: var(--color-text-secondary); margin-bottom: 2rem; }

	.feature-list { list-style: none; padding: 0; margin: 0 0 3rem 0; flex-grow: 1; }
	.feature-list li { margin-bottom: 1rem; padding-left: 1.5rem; position: relative; color: var(--color-text-secondary); font-size: 0.95rem; }
	.feature-list li::before { content: '▨'; position: absolute; left: 0; color: var(--color-accent); }
	
	.w-full { width: 100%; }

	/* Protocols Showcase */
	.protocols-showcase { padding: var(--space-xl) 0; border-top: 1px solid var(--color-border); }
	.section-desc { color: var(--color-text-secondary); max-width: 600px; margin-bottom: var(--space-xl); }

	.protocol-story {
		display: grid;
		grid-template-columns: 1fr 300px;
		gap: var(--space-xl);
		padding: var(--space-lg) 0;
		border-bottom: 1px solid var(--color-border);
		align-items: center;
	}

	.protocol-story.last { border-bottom: none; }

	.protocol-num {
		font-family: monospace;
		color: var(--color-accent);
		font-size: 0.8rem;
		opacity: 0.5;
		display: block;
		margin-bottom: 0.5rem;
	}

	.protocol-story h3 { font-size: 2rem; margin-bottom: 1.5rem; }
	.story-pain, .story-heaven { margin-bottom: 1rem; line-height: 1.6; font-size: 1rem; }
	.story-pain strong { color: #ff4d4d; }
	.story-heaven strong { color: #4dff4d; }

	.story-stat {
		padding: var(--space-lg);
		border: 1px solid var(--color-border);
		border-radius: 12px;
		text-align: center;
		background: rgba(255, 255, 255, 0.02);
	}

	.stat-value {
		font-family: var(--font-heading);
		font-size: 2rem;
		font-weight: 900;
		color: var(--color-accent);
		margin-bottom: 0.5rem;
	}

	.stat-label { font-size: 0.75rem; color: var(--color-text-secondary); line-height: 1.4; }

	@media (max-width: 992px) {
		.hero-grid { grid-template-columns: 1fr; text-align: center; }
		.hero-desc { margin: var(--space-md) auto; }
		.hero-cta-group { justify-content: center; }
		.value-grid { grid-template-columns: 1fr; }
		.value-column.featured { transform: none; }
		.protocol-story { grid-template-columns: 1fr; }
		.story-stat { margin-top: var(--space-md); }
	}
	@media (max-width: 640px) {
		.icp-grid { grid-template-columns: 1fr; }
		.split-header { flex-direction: column; align-items: flex-start; gap: 1rem; }
	}
</style>
