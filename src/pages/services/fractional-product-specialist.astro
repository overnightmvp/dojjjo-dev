---
import Layout from '../../layouts/Layout.astro';
---

<Layout title="Fractional Product Specialist" description="Stop hiring junior PMs. Hire a Fractional CPO who codes. The bridge between vision and execution.">
	<main class="container animate-fade-in">
		<header class="service-header">
			<p class="section-meta">CORE_OFFER // PRODUCTION</p>
			<h1 class="text-gradient">Fractional Product Specialist</h1>
			<p class="hero-subtitle">We build the product, design the system, and execute the roadmap.</p>
		</header>

		<!-- THE SERVICE ARCHITECTURE -->
		<section class="blueprint-grid">
			<div class="phase surface">
				<div class="phase-number">01 // ARCHITECTURE</div>
				<h3>Strategy & Design</h3>
				<p class="phase-desc">We don't just 'make it look pretty'. We design for scalability and market authority.</p>
				<ul>
					<li>Product Roadmap & Vision</li>
					<li>User Journey Mapping</li>
					<li>High-Fidelity UI/UX (Figma)</li>
					<li>Systematic Design Systems</li>
				</ul>
			</div>

			<div class="phase surface">
				<div class="phase-number">02 // CONSTRUCTION</div>
				<h3>Full-Stack Execution</h3>
				<p class="phase-desc">Clean code. Modern stacks. Rapid shipping. We build at the speed of thought.</p>
				<ul>
					<li>Rapid MVP Prototyping</li>
					<li>Production-Grade Dev (Astro/Next)</li>
					<li>API Architecture & Integration</li>
					<li>AI Workflow Automation</li>
				</ul>
			</div>

			<div class="phase surface">
				<div class="phase-number">03 // OPTIMIZATION</div>
				<h3>Growth & Scale</h3>
				<p class="phase-desc">Shipping is just the beginning. We optimize for conversion and authority.</p>
				<ul>
					<li>CRO & Analytics Integration</li>
					<li>Technical Team Leadership</li>
					<li>Entity Schema Verification</li>
					<li>Iterative Sprint Management</li>
				</ul>
			</div>
		</section>

        <!-- CASE STUDIES SECTION -->
        <section class="section line-h">
            <div class="section-meta">SELECTED_PRODUCTION_LINES</div>
            <h2 class="text-gradient">Battle-Tested Infrastructure</h2>
            <div class="case-studies-grid">
                <div class="case-card surface">
                    <div class="case-header">
                        <span class="case-tag">INTERNAL_PRODUCT</span>
                        <h3>Project: Caption</h3>
                    </div>
                    <p>Building the world's first AI Visibility Agency. We designed the technical protocol, built the automated audit tools, and established the market category from zero.</p>
                    <div class="case-results">
                        <span>✔ Multi-Step Funnel</span>
                        <span>✔ Terminal Simulation</span>
                        <span>✔ 100% Agent Native</span>
                    </div>
                </div>

                <div class="case-card surface">
                    <div class="case-header">
                        <span class="case-tag">CLIENT_PRODUCTION</span>
                        <h3>Project: Twine</h3>
                    </div>
                    <p>Total visual and technical overhaul of a premium community platform. Developed a custom CSS design system and theme architecture for infinite scalability.</p>
                    <div class="case-results">
                        <span>✔ Custom CSS Framework</span>
                        <span>✔ Performance Optimized</span>
                        <span>✔ Modular Architecture</span>
                    </div>
                </div>
            </div>
        </section>

		<section class="pricing section">
			<div class="section-meta text-center" style="display: block;">INVESTMENT_MODELS</div>
			<h2 class="text-center text-gradient">Retain the Specialist</h2>
			<div class="pricing-grid">
				<div class="price-card">
					<div class="tier-label">THE_BUILDER</div>
					<div class="price">$8,000<span>/mo</span></div>
					<p class="price-desc">Focused execution for early-stage founders needing velocity.</p>
					<ul>
						<li>Full-Stack Development</li>
						<li>Systematic UI Design</li>
						<li>Weekly Production Sprints</li>
						<li>Direct Slack Access</li>
					</ul>
					<a href="/audit" class="btn btn-outline" data-astro-prefetch>Initialize Build</a>
				</div>

				<div class="price-card featured">
                    <div class="badge">FRACTIONAL_CPO</div>
					<div class="tier-label accent">THE_COMMANDER</div>
					<div class="price">$15,000<span>/mo</span></div>
					<p class="price-desc">Full product leadership. We own the roadmap and the results.</p>
					<ul>
						<li>Strategic Roadmap Ownership</li>
						<li>End-to-End Design & Dev</li>
						<li>Technical Team Management</li>
						<li>Authority Loop Integration</li>
					</ul>
					<a href="/audit" class="btn btn-primary" data-astro-prefetch>Retain Specialist</a>
				</div>
			</div>
		</section>
	</main>
</Layout>

<style>
	main { padding: var(--space-xl) 0; }
	.service-header { margin-bottom: var(--space-xl); text-align: center; }
	.hero-subtitle { font-size: 1.5rem; color: var(--color-text-secondary); max-width: 800px; margin: 1rem auto; }

	.blueprint-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--space-md);
		margin-bottom: var(--space-xl);
	}

	.phase {
		padding: var(--space-lg);
		border: 1px solid var(--color-border);
		border-radius: 12px;
		background: var(--color-surface);
        transition: var(--transition-smooth);
	}
    .phase:hover { border-color: var(--color-accent); transform: translateY(-5px); }

	.phase-number {
		font-family: monospace;
		font-weight: 800;
		color: var(--color-accent);
		font-size: 0.75rem;
		margin-bottom: 1rem;
        letter-spacing: 0.1em;
	}

	.phase h3 { margin-bottom: 0.5rem; }
    .phase-desc { font-size: 0.85rem; color: var(--color-text-secondary); margin-bottom: 1.5rem; }

	.phase ul {
		list-style: none;
		padding: 0;
		color: var(--color-text-secondary);
	}

	.phase li {
		margin-bottom: 0.75rem;
		display: flex;
		align-items: center;
        font-size: 0.9rem;
	}

	.phase li::before {
		content: '->';
		margin-right: 0.5rem;
		color: var(--color-accent);
		font-size: 0.75rem;
        opacity: 0.5;
	}

    /* Case Studies */
    .case-studies-grid { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-lg); margin-top: var(--space-lg); }
    .case-card { padding: var(--space-lg); display: flex; flex-direction: column; gap: 1rem; }
    .case-tag { font-family: monospace; font-size: 0.7rem; color: var(--color-accent); opacity: 0.7; }
    .case-card p { color: var(--color-text-secondary); font-size: 0.95rem; line-height: 1.6; }
    .case-results { display: flex; flex-wrap: wrap; gap: 1rem; margin-top: auto; }
    .case-results span { font-size: 0.75rem; color: #fff; background: rgba(255, 255, 255, 0.05); padding: 0.25rem 0.5rem; border-radius: 4px; }

	.pricing-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: var(--space-lg);
		max-width: 900px;
		margin: var(--space-lg) auto;
	}

	.price-card {
		padding: var(--space-xl) var(--space-lg);
		border: 1px solid var(--color-border);
		border-radius: 16px;
		background: var(--color-bg);
		display: flex;
		flex-direction: column;
	}
    
    .price-card.featured {
		border-color: var(--color-accent);
		transform: scale(1.05);
		position: relative;
        background: var(--color-surface);
        box-shadow: 0 0 40px var(--color-accent-glow);
        z-index: 10;
	}

    .badge {
		position: absolute;
		top: -12px;
		left: 50%;
		transform: translateX(-50%);
		background: var(--color-accent);
		color: white;
		padding: 0.25rem 1rem;
		border-radius: 100px;
		font-size: 0.7rem;
		font-weight: 800;
        letter-spacing: 0.1em;
	}

    .tier-label { font-family: monospace; font-size: 0.75rem; color: var(--color-text-secondary); margin-bottom: 1rem; letter-spacing: 0.1em; }
    .tier-label.accent { color: var(--color-accent); }

	.price {
		font-size: 3.5rem;
		font-weight: 900;
		margin-bottom: 0.5rem;
	}

	.price span { font-size: 1.25rem; color: var(--color-text-secondary); font-weight: 400; }
    .price-desc { font-size: 0.9rem; color: var(--color-text-secondary); margin-bottom: 2rem; }

	.price-card ul {
		margin: var(--space-md) 0;
		padding: 0;
		list-style: none;
		flex-grow: 1;
	}

	.price-card li {
		margin-bottom: 1rem;
		color: var(--color-text-secondary);
        font-size: 0.95rem;
        display: flex;
        align-items: center;
	}
    .price-card li::before { content: '✔'; color: var(--color-accent); margin-right: 0.75rem; }

	@media (max-width: 992px) {
		.blueprint-grid { grid-template-columns: 1fr; }
        .case-studies-grid { grid-template-columns: 1fr; }
		.pricing-grid { grid-template-columns: 1fr; }
        .price-card.featured { transform: none; }
	}
</style>
